---
permalink: /sitemap.xml
layout: bare
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.sitemaps.org/schemas/sitemap/0.9 http://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd" xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{%- assign cd = site.collections_dir | append: "/" | replace: "//", "/" -%}
{%- assign lang_code = site.data.language_map[site.language] | default: site.language | replace: "-", "_" -%}
{%- assign lang_sitemap_key = "sitemap_" | append: lang_code -%}
{%- assign lang_sitemap = site.data[lang_sitemap_key] -%}
{%- assign en_sitemap = site.data.sitemap_en -%}
{%- assign default_lastmod = site.time | date_to_xmlschema -%}
{%- for page in site.documents -%}
  {%- assign hide_url = page.hidden -%}
  {%- if page.collection == 'site_pages' -%}
    {%- assign hide_url = false -%}
  {%- endif -%}
  {%- if hide_url == nil  -%}
    {%- assign col_info = site.collections | where:"label", page.collection | first -%}
    {%- if col_info.hidden -%}
      {%- assign hide_url = col_info.hidden -%}
    {%- endif -%}
  {%- endif -%}
  {% unless hide_url or page.layout == 'redirect' or page.layout == 'blank_config' or page.noindex == true or page.config_only == true %}
  {%- assign sitemap_key = page.relative_path | remove_first: cd -%}
  <url>
    <loc>{{site.homeurl}}{{site.baseurl}}{{ page.url }}</loc>
    {%- if lang_sitemap and lang_sitemap[sitemap_key] %}
    <lastmod>{{ lang_sitemap[sitemap_key] }}</lastmod>
    {%- elsif en_sitemap and en_sitemap[sitemap_key] %}
    <lastmod>{{ en_sitemap[sitemap_key] }}</lastmod>
    {%- else %}
    <lastmod>{{ default_lastmod }}</lastmod>
    {%- endif %}
  </url>
  {%- endunless -%}
{%- endfor -%}
</urlset>
