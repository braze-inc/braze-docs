{% comment %}
  식별자 필드 수준 암호화 및 PII에 대한 설명입니다. 필드 수준 암호화 문서 및 릴리스 노트에서 사용하세요.
  매개변수:
  - 링크(선택 사항): 설정하면 "식별자 필드 수준 암호화"가 이 링크(e.g)로 래핑됩니다. {{site.baseurl}}/user_guide/analytics/field_level_encryption/).
{% endcomment %}
{% if include.link %}
식별자 필드 수준 암호화]({{ site.baseurl }}/{{ include.link }}) 를 사용하면 이메일 주소를 AWS 키 관리 서비스(KMS)로 원활하게 암호화하여 Braze에서 공유되는 개인 식별 정보(PII)를 최소화할 수 있습니다. 암호화는 민감한 데이터를 읽을 수 없는 암호화된 정보인 암호 텍스트로 대체합니다.
{% else %}
식별자 필드 수준 암호화를 사용하면 AWS 키 관리 서비스(KMS)로 이메일 주소를 원활하게 암호화하여 Braze에서 공유되는 개인 식별 정보(PII)를 최소화할 수 있습니다. 암호화는 민감한 데이터를 읽을 수 없는 암호화된 정보인 암호 텍스트로 대체합니다.
{% endif %}
